# GFX.Audio tests

`Audio.sx` and `AudioNative.sx` validate the package implementation directly.
`Consumer` is an anonymous application workspace executed from its own
directory to verify the public package, catalog and native-provider boundaries.
